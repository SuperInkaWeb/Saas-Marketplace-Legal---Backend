spring:
  application:
    name: legit
  profiles:
    active: prod
  flyway:
    placeholder-replacement: false
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: ${PORT:8080}

app:
  security:
    jwt:
      header: "Authorization"
      prefix: "Bearer "